# Guardian Frontend

Minimal Vite + React frontend. The app reads the backend URL from `.env` using the
`VITE_API_URL` variable.

Quick start:

```bash
cd client/frontend
npm install
npm run dev
```

Change the backend URL in `.env` if your Flask server runs on a different port.
